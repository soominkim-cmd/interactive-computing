# AGENTS.md — Chat agent instructions for this workspace

Purpose
-------
This short file tells AI coding agents how to be immediately productive in this repository (p5.js sketch folders named `WK*`). Keep it minimal and link-first.

Quick facts
-----------
- Project: collection of p5.js sketches organized under `WK2`..`WK14` (each contains `index.html`, `sketch.js`, `style.css`).
- How to run locally: serve the repo root and open a `WK*/index.html` in a browser.

Recommended local commands
--------------------------
Run a simple static server (works on macOS):

```bash
cd "$(dirname "$0")"
python3 -m http.server 8000
# then open http://localhost:8000/WK8/ (or any WK folder)
```

Agent conventions (minimal, actionable)
--------------------------------------
- Edits: prefer small, targeted patches. Use `apply_patch` (or the repo's preferred edit API) and include only the minimal changes needed.
- Style: preserve existing file structure and formatting. Do not reformat unrelated files.
- Tests: there are no automated tests—validate changes by running the local server and visually checking the affected `WK*/index.html`.
- Documentation: do not copy large docs into this file; link to existing files when they exist.

Codex setting guidance (argument: "codex setting")
--------------------------------------------------
When the user requests a `codex`-style assistant behavior, follow these rules:

- Be conservative: prefer to ask for confirmation before making large changes across multiple `WK*` folders.
- Make edits in small, reviewable commits/patches and explain the intent in the commit message.
- When generating code, include a brief usage example and a one-line manual test the user can run (e.g., which `index.html` to open).
- If the change affects visual output (sketch behavior, sizes, colors), include a screenshot suggestion and exact file(s) changed.

What agents should look for first
--------------------------------
1. Top-level `README.md`, `CONTRIBUTING.md`, or existing `.github/*` customization files (link rather than duplicate).
2. Repeated patterns across `WK*` folders (common `sketch.js` structure) — prefer refactoring only after user approval.

If you make changes
-------------------
- Update this file only if you add new conventions or automated commands.
- When in doubt, leave a short note in the PR description explaining why the change was required.

Next agent-customizations to consider
-----------------------------------
- A dedicated `.github/copilot-instructions.md` that references this file and maps common tasks (run/build/test) to commands.
- A small skill that knows how to run the local server and open a given `WK` folder in the browser.

---
Generated by an agent to help collaborators and future AI assistants. Ask the user if they want a `.github/copilot-instructions.md` as well.
